machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t mrzizik/sample-node .

test:
  override:
    - docker run mrzizik/sample-node npm test
deployment:
  production:
    branch: master
    commands:
      - chmod +x deploy.sh
      - sh ./deploy.sh