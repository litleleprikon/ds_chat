machine:
  services:
    - docker

dependencies:
  override:
    - ps

test:
  override:
    - ps
deployment:
  production:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - chmod +x deploy.sh
      - sh ./deploy.sh